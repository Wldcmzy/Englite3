<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Englite3"
        tools:targetApi="31" >
        <activity
            android:name=".activities.WordRecite"
            android:exported="false" />
        <activity
            android:name=".activities.WordDatabaseInfo"
            android:exported="false" />
        <activity
            android:name=".activities.CloudSettingUserInfo"
            android:exported="false" />
        <activity
            android:name=".activities.CloudSettingRemote"
            android:exported="false" />
        <activity
            android:name=".activities.CloudQuery"
            android:exported="false" />
        <activity
            android:name=".activities.More"
            android:exported="false" />
        <activity
            android:name=".activities.Main"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>